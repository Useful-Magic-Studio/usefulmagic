/**
 * Some Vercel project settings run:
 *   node .v0/inject-built-with-v0.mjs && next build
 * This repository does not depend on the original v0 injection step,
 * but the file must exist for the build command to succeed.
 */
